import React from "react";
import styles from "./HTML.module.css";

const HTML = () => {
  return (
    <div className={styles.container}>
      <h2>HTML</h2>
      <p>
        HTML steht für Hypertext Markup Language und wird verwendet, um die
        Struktur von Webseiten zu definieren.
      </p>
    </div>
  );
};

export default HTML;
